# Tauri Ignore File
# Files and patterns to exclude from bundling

# Development tools (built during beforeBuildCommand but not needed in app)
generate-bindings
generate-bindings.exe

# Metadata files from bin directory
bin/binary-dependencies.json
bin/README.md
