name: Build & Sign ykman Bundles

# Manual trigger to build and sign ykman for all platforms
# Publishes signed binaries to barqly-vault-dependencies release
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'ykman version to build'
        required: true
        default: '5.8.0'
      update_release:
        description: 'Update barqly-vault-dependencies release'
        type: boolean
        default: false  # Manual upload recommended

jobs:
  build-ykman:
    strategy:
      matrix:
        include:
          # macOS Intel (use macos-13 for x86_64)
          - os: macos-13
            platform: darwin
            arch: x86_64
            platform_name: darwin-x86_64
          # macOS Apple Silicon (macos-latest is ARM64)
          - os: macos-latest
            platform: darwin
            arch: arm64
            platform_name: darwin-arm64
          # Linux
          - os: ubuntu-22.04
            platform: linux
            arch: x86_64
            platform_name: linux-x86_64
          # Windows
          - os: windows-latest
            platform: windows
            arch: x86_64
            platform_name: windows-x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      # Setup Apple certificates for macOS signing
      - name: Setup Apple Certificates
        if: matrix.platform == 'darwin'
        uses: ./.github/actions/apple-certificates
        with:
          certificate-p12: ${{ secrets.APPLE_CERTIFICATE }}
          certificate-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          operation: setup

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libpcsclite-dev swig

      - name: Build ykman
        shell: bash
        run: |
          chmod +x scripts/yubikey/build-ykman.sh
          ./scripts/yubikey/build-ykman.sh ${{ github.event.inputs.version }}

      # Sign macOS binaries with Developer ID
      - name: Sign macOS binary
        if: matrix.platform == 'darwin'
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_DEVELOPER_ID }}
        run: |
          echo "🔐 Signing ykman binary..."

          # The keychain was created by apple-certificates action
          KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"

          # List available signing identities from the created keychain
          echo "Available signing identities:"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

          # Check what was built
          echo "Checking build output:"
          ls -la src-tauri/bin/darwin/
          ls -la src-tauri/bin/darwin/ykman-bundle/ || echo "❌ ykman-bundle directory not found"

          YKMAN_BINARY="src-tauri/bin/darwin/ykman-bundle/ykman"

          if [ ! -f "$YKMAN_BINARY" ]; then
            echo "❌ ykman binary not found at $YKMAN_BINARY"
            echo "Directory contents:"
            find src-tauri/bin/darwin -type f -name "ykman*" || echo "No ykman files found"
            exit 1
          fi

          echo "✍️ Signing: $YKMAN_BINARY"
          file "$YKMAN_BINARY"

          # Sign the binary using the specific keychain
          /usr/bin/codesign \
            --force \
            --sign "$APPLE_SIGNING_IDENTITY" \
            --options runtime \
            --timestamp \
            --keychain "$KEYCHAIN_PATH" \
            "$YKMAN_BINARY"

          # Verify signature
          echo "🔍 Verifying signature..."
          /usr/bin/codesign --verify --strict --verbose=2 "$YKMAN_BINARY"
          /usr/bin/codesign -dv --verbose=4 "$YKMAN_BINARY" 2>&1 | grep -E "Authority|Identifier"

          echo "✅ macOS binary signed successfully"

      - name: Package ykman bundle
        shell: bash
        run: |
          cd src-tauri/bin/${{ matrix.platform }}
          tar -czf ../../../ykman-${{ github.event.inputs.version }}-${{ matrix.platform_name }}.tar.gz ykman-bundle
          cd ../../..

          echo "📦 Created tarball:"
          ls -lh ykman-*.tar.gz

          # Calculate checksum
          if [[ "${{ matrix.platform }}" == "darwin" ]] || [[ "${{ matrix.platform }}" == "linux" ]]; then
            shasum -a 256 ykman-*.tar.gz
          else
            sha256sum ykman-*.tar.gz
          fi

      - name: Upload ykman bundle
        uses: actions/upload-artifact@v4
        with:
          name: ykman-${{ matrix.platform_name }}
          path: ykman-*.tar.gz
          retention-days: 7

      # Cleanup Apple keychain
      - name: Cleanup Certificates
        if: always() && matrix.platform == 'darwin'
        uses: ./.github/actions/apple-certificates
        with:
          operation: cleanup

  # Update barqly-vault-dependencies release with signed binaries
  update-dependencies-release:
    needs: build-ykman
    if: github.event.inputs.update_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/ykman-artifacts

      - name: Organize artifacts
        run: |
          mkdir -p dist/binaries
          find dist/ykman-artifacts -name "*.tar.gz" -exec mv {} dist/binaries/ \;
          cd dist/binaries

          echo "📋 Artifacts to upload:"
          ls -lh ykman-*.tar.gz

          echo "🔐 Generating checksums..."
          shasum -a 256 ykman-*.tar.gz > ykman-checksums.txt
          cat ykman-checksums.txt

      - name: Update barqly-vault-dependencies release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cd dist/binaries

          RELEASE_TAG="barqly-vault-dependencies"

          echo "📤 Uploading ykman binaries to $RELEASE_TAG release..."

          # Upload each file (gh release upload handles duplicates by replacing)
          for file in ykman-*.tar.gz ykman-checksums.txt; do
            echo "Uploading $file..."
            gh release upload "$RELEASE_TAG" "$file" --clobber
          done

          echo "✅ Updated $RELEASE_TAG release with signed ykman binaries"
          echo ""
          echo "📝 Next steps:"
          echo "1. Update src-tauri/bin/binary-dependencies.json with new checksums"
          echo "2. Test with: gh workflow run release.yml -f version=0.2.0-test.X"
